spring:
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master-test.xml
    enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5433/ya_blog
    username: root
    password: root
    driver-class-name: org.postgresql.Driver

post:
  image:
    dir: src/test/resources/image/to_be_deleted
